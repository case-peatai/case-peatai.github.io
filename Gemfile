# frozen_string_literal: true

source "https://rubygems.org"

# This ensures your local site matches GitHub Pages exactly
gem "github-pages", group: :jekyll_plugins

gem "jekyll-feed", "~> 0.12"
gem "jekyll-sitemap"
gem "jekyll-seo-tag"

# Optional: Add this if you want to use the default Jekyll theme as a base
gem "minima", "~> 2.5"

# Windows-specific fix to prevent common errors
gem "webrick", "~> 1.7"
# gem "rails"
